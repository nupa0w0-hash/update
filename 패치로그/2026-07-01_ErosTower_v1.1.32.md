# Eros Tower v1.1.32

- `원문 병행` 출력 조립에서 `[원문]`, `[번역]` 라벨을 제거했다.
- 원문 병행은 이제 문단/대사 단위로 `원문 -> 번역 -> 원문 -> 번역` 순서만 유지하고, 별도 제목 라벨을 붙이지 않는다.
- 모델이나 기존 출력이 `[원문]`, `[번역]`, `[병행]`, `[Translation]` 같은 라벨을 내보내도 기본 품질 정규식에서 제거하도록 보루 규칙을 추가했다.
- 번역 프롬프트 프리셋은 수정하지 않았다.
